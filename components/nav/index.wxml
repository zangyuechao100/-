<!--components/nav/index.wxml-->
<view class="container">
  <image bind:tap="onLeft" class="icon" src="{{lastest ? disLeftSrc : leftSrc}}" />
  <text class="title">{{title}}</text>
  <image bind:tap="onRight" class="icon" src="{{first ? disRightSrc : rightSrc}}" />
</view>
